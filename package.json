{
  "private": true,
  "name": "args-typed",
  "version": "0.0.2",
  "description": "Argument parser with full static typing in TypeScript",
  "repository": {
    "type": "git",
    "url": "https://github.com/basic-examples/args-typed.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc && cpy README.md dist && package-json-minifier",
    "publish": "npm run build && cd dist && npm publish",
    "test": "cd test && npm start"
  },
  "devDependencies": {
    "@types/node": "^24.0.14",
    "cpy-cli": "^5.0.0",
    "package.json-minifier": "^0.0.4",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}

{
  "private": true,
  "name": "@this-project/sample",
  "scripts": {
    "build": "cd .. && npm run build",
    "prestart": "npm run build && npm install",
    "start": "bash test.bash"
  },
  "devDependencies": {
    "@types/node": "^24.0.14",
    "args-typed": "file:../dist",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
